# CarryMem 遗留问题全量盘点与解决计划 (v0.10.1)

> **日期**: 2026-09-07
> **性质**: 活文档 — 每解决一项立即更新状态
> **前置**: [V0.10.1_PROJECT_REVIEW.md](V0.10.1_PROJECT_REVIEW.md)（七维度评估 + P0/P1 已修复项）
> **共识状态**: 待用户确认后按批次推进（P0-hotfix 已即时执行，见 §1）

---

## 0. 盘点方法

在评估报告 §6 已知 P2 之外，本轮做了补充扫描：glama.json 等清单、ci.yml 全部 continue-on-error、TD 计划 P3 区与"待用户决策"区、src/ 吞错模式、TODO/FIXME、scripts/extensions/integrations/bin 目录、dependabot 现状、**本地起临时 venv 复刻 CI 锁定版本跑真实四门禁**（radon 6.0.1 / flake8 7.3.0 / black 26.5.1 / isort 6.1.0 / mypy 2.3.0 + textual/aiosqlite 复刻 CI mypy 环境）。

**核心新发现：v0.10.0/v0.10.1 两个发布提交带入了 red 质量门禁**（black 8 文件 / flake8 5 错 / isort 2 文件），即主分支 CI lint 门禁在发布时已是红的——发布纪律失守是最大遗留问题。

---

## 1. P0-hotfix（已即时执行，等全量回归后提交）

| # | 问题 | 证据 | 解决 | 状态 |
|---|---|---|---|---|
| H1 | lint 门禁红：black 8 文件、flake8 5 错（含 `__main__.py` 缺文件尾换行）、isort 2 文件 — 全部为 v0.10.0/0.10.1 提交带入，非本轮改动 | 本地 CI 锁定版本实测 | black/isort 自动重排（8 files, +42/−51），flake8 归零；全量回归 4854 passed / 0 failed 证明零行为变化 | ✅ 已修 |
| H2 | radon 门禁与硬约束不符：项目记忆规定 "complexity ≥21 (D+) blocking"，实际 CI 只查 E/F（`-n E`）且 continue-on-error: true | ci.yml L182-194 | 重构唯一 D 级函数 `upgrade_to_rule`（cc 24→10，拆 `_map_level_params`/`_find_existing_auto_rule`/`_upsert_auto_rule` 三助手，异常契约逐路径保持），41 相关测试通过；门禁翻转 `-n D` + blocking | ✅ 已修 |
| H3 | mypy "8 errors" 判定纠正 | 首测 8 错为**本地缺 textual/aiosqlite 的环境伪影**；复刻 CI 环境（补装两可选依赖）后 `Success: no issues found in 162 source files` | 无需修；教训记录：mypy 结论必须在 CI 同构环境判定 | ✅ 关闭（无问题） |

## 2. 遗留问题全量清单（已知 P2 + 本轮新发现）

### A. 架构 / 代码债

| # | 问题 | 位置 | 影响 | 解决方案 | 批次 |
|---|---|---|---|---|---|
| A1 | `my_profile` 每次调用新建 RuleEngine | [system.py:51-54](../../src/carrymem/integration/layer2_mcp/handlers/system.py) | 同库多实例（与 handlers 收敛前同病） | 复用 `carrymem.rule_engine` facade 实例；+身份断言测试 | P1-B |
| A2 | DevSquadAdapter 自建 RuleEngine | [adapter.py:36](../../src/carrymem/integration/devsquad/adapter.py) | 同上 | 改为构造参数注入 facade 实例；DevSquad 侧联调验证 | P1-B |
| A3 | `AsyncCarryMem(native_async=True)` 16+ 公开 API 抛 RuntimeError（`_require_sync()`） | [async_carrymem.py](../../src/carrymem/async_carrymem.py) | 深层 API 承诺broken，误导使用者 | **决策二选一**：(a) 补齐 async 实现需 AsyncSQLiteAdapter 真异步化（MINOR 版本 v0.11.0 专项）；(b) 诚实降级：删除 `native_async` 参数与文档承诺，README 明确"async 仅 sqlite adapter I/O 层"。建议 (b)（Simplicity First），留 (a) 给 v0.11.0 评估 | P1-C |
| A4 | `AsyncSQLiteAdapter` 非 StorageAdapter 子类；`encryption_key` 静默丢弃 | [async_sqlite.py:48-77](../../src/carrymem/adapters/async_sqlite.py) | 契约破坏 + 静默不加密（安全相关） | 最小修：构造时 encryption_key 非 None 即 raise（fail-closed，禁止静默）；继承问题随 A3 决策一并处理 | P1-C |
| A5 | `monitoring._latency_buckets` 无界增长 | [monitoring/__init__.py:64-101](../../src/carrymem/monitoring/__init__.py) | 长驻进程内存泄漏 | 固定 bucket 边界（0.001/0.01/0.1/1/10s …）+ deque(maxlen)；该模块半幽灵（见 D1），若 D1 决定删除则此项随销 | P1-B |
| A6 | `backup.py` 3 处 `except…pass` | [backup.py:47,81,122](../../src/carrymem/backup.py) | 审查结论：均为 chmod best-effort / 临时文件清理，**可接受** | 仅补注释说明吞错理由（防未来读者误判），不改行为 | P1-B |

### B. 半幽灵模块去留（需用户决策）

| # | 模块 | 现状 | 选项 | 建议 |
|---|---|---|---|---|
| B1 | `plugins/__init__.py` | 自声明 ghost，零接线 | (a) 删除 (b) 接线 | **删**（v0.11.0 PATCH 专属，禁 feature bloat） |
| B2 | `layers/summary_layer.py` | 生产实际用 `format.py` 的 RuleBasedSummarizer | (a) 删除 (b) 替换生产路径 | **删**（含其专属测试与文档引用清理） |
| B3 | `monitoring/` | 半幽灵 + A5 泄漏 | (a) 删除 (b) 接入 /metrics 端点 | **删**（无消费者；将来需要时按需重建，git 历史可寻回） |
| B4 | `integration/devsquad/adapter.py` | 自建 RuleEngine（A2）+ DevSquad 宿主联调证据存于 DevSquad 仓 | 保留接线并修 A2 | **保留 + 修 A2**（跨仓协作入口，非幽灵） |

### C. CI/CD 与发布纪律

| # | 问题 | 位置 | 解决方案 | 批次 |
|---|---|---|---|---|
| C1 | **dependabot 已被禁用**（commit 6e15b4d, 2026-08-19 用户决策），但项目记忆 4 条 dependabot 硬约束仍声明生效；运行时依赖安全更新通道因此只剩 CI 内 pip-audit（push/PR 时点扫描，非每日 advisory PR） | `.github/dependabot.yml` 不存在 | **决策三选一**：(a) 恢复 `security-updates` 专用 ecosystem（每日安全 PR、零版本噪音，与"只允许 patch"决策兼容）；(b) 维持禁用，把记忆约束改为"禁用状态 + pip-audit 通道"，并接受窗口期风险；(c) 维持禁用 + 每周六人工 `pip-audit` 例行扫描并入周六例行任务。**建议 (a)** | **待用户决策** |
| C2 | vscode-e2e 编译脚本 TODO 未关闭（不阻塞发布但 job 长期红/灰） | release.yml L175 TODO + extensions/vscode-carrymem | 修 `copy package.json to out/` 后重新纳入 needs 链；或明确降级为 nightly-only 并在 RUNBOOK 标注（v13 记录 17/17 passing，说明主问题已修，只剩编译脚本收尾） | P1-B |
| C3 | 其余 4 个 advisory job（Syntax Check / i18n / Docs Check / Optional Deps） | ci.yml L23,203,292,312 | 逐个评估：Syntax Check 建议转 blocking（成本 0）；i18n/Docs 维持 advisory 但在 job 名注明豁免理由；Optional Deps 依赖 nightly 结果再定 | P1-B |
| C4 | CI 首跑观察：本轮 push（a520999 + 热修提交）的 lint 门禁是否全绿 | GitHub Actions | 观察 run 结果；红则按报告修复（预期全绿，本地已复刻验证） | 进行中 |

### D. 文档 / 流程债

| # | 问题 | 位置 | 解决方案 | 批次 |
|---|---|---|---|---|
| D1 | ROADMAP_P0_P3 汇总表 "P3 0%" 与 TECH_DEBT_PLAN P3 十项全 ✅ 矛盾 | [ROADMAP_P0_P3.md:37](../ROADMAP_P0_P3.md) | 同步进度表（P3 10/10 100%） | P1-B |
| D2 | TD-010 状态行缺失（test_sqlite_adapter.py 实存 112 passed） | TECH_DEBT_PLAN.md:159 | 补 ✅ 状态行 + 覆盖率实测值 | P1-B |
| D3 | TD 计划"待用户决策：TD-015 PyPI token 轮换"悬置 | TECH_DEBT_PLAN.md:1047 | 按用户安全规则（2026-08-20 轮换判定标准）：token 仅在本机对话/CI secret 出现、未进公开渠道 → **不需轮换**；在 TD-015 记录此判定并关闭 | P1-B |
| D4 | "P2 技术债 51.9%/P3 0%" 等进度快照过时 | ROADMAP_P0_P3.md 头部 | 同步最新计数 | P1-B |
| D5 | README 营销数字（"93x faster than Mem0"/88%）无随附基准脚本引用 | README.md | 每个数字后附 benchmarks/ 复现命令（`tests/test_performance_benchmark.py` + CI artifact），不可复现的标注来源与日期 | P2 |
| D6 | 发布前检查单需固化"本地四门禁预跑"步骤（本轮 lint 红 2 个版本未被发现即根因） | CLAUDE.md 工作流节 + docs/runbooks/ | 在 CLAUDE.md 提交前清单加入"本地 venv 复刻 CI 锁定版本跑 black/isort/flake8/mypy/radon"（脚本化：scripts/ci_local_check.py 已存在，核实其对齐锁定版本后文档引用它） | P1-B |

### E. 环境 / 工具债（低优先）

| # | 问题 | 解决 |
|---|---|---|
| E1 | 本地沙箱无 dev extras（pytest-timeout 缺失等） | 建一次性 `/tmp` venv 装 `.[dev]`（PEP 668 不阻 venv）；或 `scripts/install.sh` 文档化 |
| E2 | 本轮 lint venv 为临时性 | 将 `scripts/ci_local_check.py` 升级为可复现的本地门禁脚本（安装锁定版本 + 依次跑五门禁）并入库 |

## 3. 推进批次建议

| 批次 | 内容 | 前置 |
|---|---|---|
| **P0-hotfix（已完成待提交）** | §1 H1/H2 + 门禁翻转 | 无 — 全量回归后即提交推送，CI 观察 |
| **P1-A（用户决策项）** | C1 dependabot、A3/B1/B2/B3 半幽灵去留 | 本文档共识 |
| **P1-B（机械修复，共识后一批做完）** | A1/A2/A5/A6、C2/C3、D1/D2/D3/D4/D6 | P1-A 决策（A5 与 B3 联动） |
| **P1-C（MINOR 专项，进 v0.11.0 规划）** | A3 async 承诺、A4 适配器契约 | 版本节奏评估 |
| **P2（渐进）** | D5、E1/E2 | 随 P1-B 顺手做 D6 |

## 4. 验证方法（每批次收口标准）

1. 本地 venv 五门禁（锁定版本）全绿：`black --check / isort --check / flake8 --count / mypy src/ / radon cc -s -n D`
2. 全量回归 `pytest tests/` 0 failed（4878 收集基线不降）
3. CI run 全绿（含新 blocking radon gate）
4. 文档同步 + 本活文档状态列更新
